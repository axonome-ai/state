# Warmup Cosine Restarts Learning Rate Policy
# This policy can help escape local minima and improve convergence by restarting
# the cosine annealing schedule periodically.

name: warmup_cosine_restarts
max_lr: 1e-4
min_lr: 1e-6
warmup_steps: 1000
T_0: 10000
T_mult: 1
eta_min_ratio: 0.1
